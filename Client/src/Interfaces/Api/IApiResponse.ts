// import { IErrors } from "./IApiErrorResponse";

// The interface responsible for Structure declaration for ApiResponse
export interface IApiResponse {
  success: boolean;
  data?: unknown;
  error?: string;
}
